name: khojmitra
description: KhojMitra.AI – Smart AI Lost & Found System for ABESIT

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  firebase_core: ^3.15.2
  firebase_auth: ^5.7.0
  cloud_firestore: ^5.6.12
  google_sign_in: ^7.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  google_fonts: ^6.2.1

  # ── Firebase ───────────────────────────────────────────
  firebase_storage: ^12.3.3
  # firebase_messaging: ^15.1.3
 
  # ── Image Picker ───────────────────────────────────────
  image_picker: ^1.1.2
 
  # ── UI & Utilities ─────────────────────────────────────
  cached_network_image: ^3.4.1
  flutter_local_notifications: ^17.2.3
  intl: ^0.19.0
  uuid: ^4.5.0
  shared_preferences: ^2.3.3

flutter:
  uses-material-design: true

  